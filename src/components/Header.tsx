import { FC } from "react";

export const Header: FC = () => {
    return (
        <div className="flex flex-row w-full backdrop-filter backdrop-blur-lg text-white fixed">
            test
        </div>
    );
}